<UserControl x:Class="CyberSecurityBot.Wpf.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:CyberSecurityBot.Wpf.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="ChatScroll" Grid.Row="0" Margin="0,0,0,12"
                      VerticalScrollBarVisibility="Auto" Background="#FF111111">
            <ItemsControl x:Name="MessagesList" ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:MessageBubble/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputBox" Grid.Column="0"
                     Style="{StaticResource ChatTextBox}"
                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="500"
                     KeyDown="InputBox_KeyDown"/>
            <Button Grid.Column="1"
                    Content="Send"
                    Margin="8,0,0,0"
                    Style="{StaticResource SendButton}"
                    Command="{Binding SendCommand}"/>
        </Grid>
    </Grid>
</UserControl>
