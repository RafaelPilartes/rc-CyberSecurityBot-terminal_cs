<UserControl x:Class="CyberSecurityBot.Wpf.Views.QuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Score / progress bar (structure only) -->
        <TextBlock Grid.Row="0" Text="{Binding ScoreText}"
                   Foreground="{StaticResource PrimaryBrush}" FontFamily="{StaticResource MonoFont}"
                   FontSize="14" Margin="0,0,0,12"/>

        <!-- Question + options (structure only) -->
        <Border Grid.Row="1" Background="#FF111111" Padding="16" CornerRadius="6"
                BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding QuestionText}" TextWrapping="Wrap"
                           Foreground="{StaticResource PrimaryBrush}" FontFamily="{StaticResource MonoFont}"
                           FontSize="16" Margin="0,0,0,16"/>

                <ItemsControl ItemsSource="{Binding Options}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" Margin="0,0,0,8" HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Left"
                                    Style="{StaticResource SendButton}"
                                    Command="{Binding DataContext.AnswerCommand,
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="{Binding FeedbackText}" TextWrapping="Wrap"
                           Foreground="{StaticResource SystemForegroundBrush}" FontFamily="{StaticResource MonoFont}"
                           Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <!-- Controls (structure only) -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Next" Margin="0,0,8,0" Style="{StaticResource SendButton}"
                    Command="{Binding NextCommand}"/>
            <Button Content="Start / Restart" Style="{StaticResource SendButton}"
                    Command="{Binding RestartCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
